@model Website.ViewModels.HomeViewModel

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center mb-4">
    <h1 class="display-4">Welcome to PartyPeople</h1>
</div>

<div class="d-flex p-2">
    <h4 class="flex-fill">Upcoming Events for the next 7-days</h4>
</div>
<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th scope="col" class="col-sm-2">Event Start</th>
                <th scope="col">Description</th>
                <th scope="col" class="col-sm-2"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var eventDetails in Model.Events)
            {
                <tr>
                    <td>@eventDetails.StartDateTime.ToString("dd/MM/yyyy HH:mm")</td>
                    <td>@eventDetails.Description</td>
                    <td class="text-end py-1">
                        <a class="btn btn-outline-primary btn-sm" role="button" asp-controller="Event" asp-action="Details" asp-route-id="@eventDetails.Id">View</a>
                    </td>
                </tr>
            }
            @if (!Model.Events.Any())
            {
                <tr>
                    <td colspan="4">No upcoming events.</td>
                </tr>
            }
        </tbody>
    </table>
</div>

<div class="d-flex p-2">
    <h4 class="flex-fill">Most Social Colleagues Leaderboard</h4>
</div>

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th scope="col" class="col-sm-2">Colleague Name</th>
                <th scope="col">Number Of Events</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var colleague in Model.TopColleagues)
            {
                <tr>
                    <td>@colleague.FirstName @colleague.LastName</td>
                    <td>@colleague.EventCount</td>
                </tr>
            }
            @if (!Model.TopColleagues.Any())
            {
                <tr>
                    <td colspan="2">No attendees found.</td>
                </tr>
            }
        </tbody>
    </table>
</div>



<div class="d-flex p-2">
    <h4 class="flex-fill">Upcoming Events with no attendees</h4>
</div>

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th scope="col" class="col-sm-2">Event Start</th>
                <th scope="col">Description</th>
                <th scope="col" class="col-sm-2"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var eventDetails in Model.EventsWithNoAttendees)
            {
                <tr>
                    <td>@eventDetails.StartDateTime.ToString("dd/MM/yyyy HH:mm")</td>
                    <td>@eventDetails.Description</td>
                    <td class="text-end py-1">
                        <a class="btn btn-outline-primary btn-sm" role="button" asp-controller="Event" asp-action="Details" asp-route-id="@eventDetails.Id">View</a>
                    </td>
                </tr>
            }
            @if (!Model.EventsWithNoAttendees.Any())
            {
                <tr>
                    <td colspan="4">No upcoming events without attendees.</td>
                </tr>
            }
        </tbody>
    </table>
</div>
